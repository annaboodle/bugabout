# Bugabout project guide

Last reviewed: 2026-08-31

## Purpose

Bugabout turns a geocaching trackable history into an animated, shareable journey. The product name is **Bugabout** and the tagline is **Follow your bug about.**

The experience should feel like a small travel documentary rather than a conventional analytics dashboard: warm, playful, map-first, story-led, and easy to share.

## Keep this file alive

Treat this file as the project's maintained working memory, not an append-only diary.

- Read it before making project changes.
- Update it in the same change whenever architecture, commands, data rules, product terminology, or important constraints change.
- Record new gotchas as soon as they are confirmed.
- Revise or remove stale notes instead of leaving contradictory instructions behind.
- Keep findings concise and actionable. Put lengthy investigations elsewhere and link them here if needed.
- Update the `Last reviewed` date after materially reviewing or revising this file.

## Current implementation

Bugabout is currently a zero-build static prototype:

- `index.html` contains the application structure and locally loaded assets.
- `styles.css` contains the full responsive visual system.
- `app.js` contains the normalized journey renderer, sample data, Leaflet setup, animation, statistics, interactions, and file-import UI.
- `kml-importer.js` converts an official Geocaching trackable KML export into Bugabout's normalized journey shape.
- `bumblebee.js` synthesises the opening figure of Flight of the Bumblebee with the Web Audio API.
- `livereload.js` is a development convenience: it polls the static server for changes to the authored files and reloads when one lands. It returns immediately on any non-local hostname, so it is inert in production, and there is no build step for it to hook into instead.
- `country-lookup.js` resolves a coordinate to a country from vendored boundaries, since KML carries no country field.
- `journey-link.js` encodes a journey into the URL fragment and reads one back, so a route can be shared without a backend.
- `vendor/countries.json` is the boundary index it loads, generated from `vendor/ne_50m_admin_0_countries.geojson` by `tools/build-countries.mjs`.
- `vendor/us-states.json` resolves a US stop to its state, generated by `tools/build-states.mjs`. `tools/lib/boundaries.mjs` holds the slimming both build scripts share.
- `fixtures/` contains real KML exports named only with their public `TB...` references for repeatable testing.
- `tests/smoke.html` and `tests/smoke.mjs` exercise the browser-only importer, boundary lookup, and journey-link modules against both fixtures. The page publishes a final `data-status`, which the headless runner reads directly instead of relying on an arbitrary sleep.
- `tools/run-smoke-tests.mjs` serves the project on an ephemeral local port and runs that suite in an installed Chromium-based browser without adding a package dependency.
- `vendor/leaflet.css` and `vendor/leaflet.js` pin Leaflet 1.9.4 locally.
- `README.md` describes local setup and the current milestone.

There is no backend, package manager, bundler, authentication layer, direct URL importer, or API integration. KML files are parsed locally in the browser and never uploaded by Bugabout.

The `tools/` scripts are one-time data preparation, not a build step. Run them only when a Natural Earth source is replaced; the committed `vendor/countries.json` and `vendor/us-states.json` are what ship. `tools/build-states.mjs` downloads its ~39 MB source to a cache outside the repo, because only the 51 US features are needed.

## Run and verify

The page reloads itself when a source file changes, so editing and checking needs no manual refresh. `livereload.js` polls `HEAD` on the authored files roughly once a second and reloads on a changed `Last-Modified`.

Start a local server from the project root:

```sh
python3 -m http.server 4173
```

Open `http://127.0.0.1:4173/`.

Minimum verification after meaningful changes:

1. Run `node tools/run-smoke-tests.mjs`. It covers both fixtures, KML parsing, country/state derivation, antimeridian data, and share-link round trips.
2. Run `node --check` on every changed JavaScript or MJS file.
3. Load the app with browser developer logs free of errors and warnings.
4. Import `fixtures/TBBDVQG.kml`; confirm 6 stops, a 1.7-mile final total, 1 country (United States), play/pause, scrubbing, and stop navigation.
5. Import `fixtures/TBA5TD9.kml`; confirm 2,472 stops, a 176,530-mile final total, 22 countries including Macao and Monaco, smooth playback, a virtualized 64-stop list window, and viewport-driven markers capped by `MARKER_BUDGET`.
6. Confirm imported countries explain their derived provenance in the readout tooltip and **Where's my KML?** dialog. Temporarily make `vendor/countries.json` unreachable and confirm the player falls back to `countries not in KML` with a clean console.
7. Test speed selection, the camera toggle, sharing, the **Where's my KML?** dialog, and KML selection and drag and drop.
8. Check at approximately 1280×720 and 390×844. Confirm the player remains visible and the page has no horizontal overflow.

## Trackable data safety

Geocaching trackables have a public reference beginning with `TB` and a separate private tracking code printed on the physical item.

- Accept, display, store, and share the public `TB...` reference only.
- Never include a private tracking code in source, fixtures, logs, URLs, screenshots, analytics, or error messages.
- Treat incoming `tracker=` query values as potentially private. Do not persist or repeat them.
- Do not imply that sample data belongs to a real trackable.
- The current sample route is fictional and must remain visibly labeled **Sample journey**. That pill is shown only for the sample: imports carry no status pill, because importing is the sole way to load a bug and a label saying so tells the user nothing.
- A production importer should use an approved Geocaching integration or an explicit user-provided KML/GPX export. Do not silently introduce page scraping.

As of 2026-08-28, Geocaching's official API is limited to approved Authorized Developers. Keep the official [API program](https://partnerships.geocaching.com/geocachingapi), [partner request form](https://partnerships.geocaching.com/apiapply), and [API License Agreement](https://partnerships.geocaching.com/api-license-agreement) as the production integration path, but do not block KML prototype progress on approval.

A URL-only importer cannot be implemented reliably in the current static frontend. Confirmed on 2026-08-28: the public trackable page is anonymously readable, but the official `/kml/tbkml.aspx?tbguid=...` endpoint returns an empty response without a signed-in Geocaching session, and Geocaching does not send cross-origin access headers that would let Bugabout read either resource from its own origin. A normal website also cannot read a file that another site downloaded without the user explicitly selecting or dropping it.

Do not work around this with server-side page parsing, authenticated scraping, shared cookies, or collected Geocaching credentials. The Groundspeak Terms of Use effective 2026-03-04 prohibit robots, spiders, scrapers, or other means of collecting service data without express written permission. Section 4.16 of the current API License Agreement likewise disallows acquiring Geocaching data through scraping or similar methods without express written consent, and section 4.22 requires each API user to authenticate through OAuth. The intended direct-import design is therefore: approved API client → user connects Geocaching via OAuth → public TB code resolves through the authorized API → normalized journey data. Confirm available trackable-history scopes and fields with Geocaching during enrollment.

## Product and interaction decisions

- The animated bug is the narrative focus; the route, current date, location, mileage, and story advance together.
- The default journey is the real `fixtures/TBA5TD9.kml`, not the fictional sample, so a first visit shows what an actual trackable's travels look like. The sample remains the offline fallback when that file cannot be fetched. The fixture therefore doubles as production content; do not delete or rename it as if it were test-only.
- Playback duration is `12s + 2.8s x sqrt(stops)`, capped at 150 seconds. It used to be linear with a 60-second cap, which every journey over about fifty stops hit, so a 50-stop and a 2,472-stop journey ran for exactly the same time and the large fixture raced past at 41 stops a second. Square root keeps short journeys brisk and gives long ones room to be watched.
- Play is the only control in `.player-transport`, so it remains the player's one visually dominant action. Speed sits in the metadata line beside `Stop N of M` as a quiet, explicit **Speed 1× ›** control; stacking a small mint `1×` chip under the play button on mobile looked like an attached badge, while an unexplained `1×` looked out of place in every layout. The readout row remains only for numbers that climb as the bug travels.
- Camera zoom is quantised to whole levels (`CAMERA_ZOOM_STEP`). With fractional zoom one frame landed on 8.2 and the next on 8.4, which reads as a glitch rather than a move; whole levels also keep tiles on their native level instead of upscaling.
- The camera holds until the bug nears the edge of the **viewport**, not of the frame it was built from. Holding against the frame failed badly: once a frame went globe-scale it contained the whole journey, so the camera never re-framed, while at minimum zoom the viewport could not display that frame and the bug spent a third of the playback off screen.
- Because everything stays visible at a wide zoom, leaving the viewport is not enough on its own — the camera would zoom out for an ocean crossing and never come back. It also re-frames when the journey warrants a view at least `CAMERA_ZOOM_REGAIN` whole levels tighter, which recovers the close view without reacting to small differences.
- Measured on the large fixture: 41 view changes across a 150-second playback, a median of 3 seconds apart, 6 under a second, and the bug never off screen. The earlier build made 82 with 48 of them under a second.
- Count camera changes by the visible tiles, not the map-pane transform. A zoom change resets that transform to zero, so watching it alone silently undercounts.
- The camera control hides itself when the whole journey already fits at zoom `CAMERA_CONTROL_MIN_ZOOM` or closer, and the map simply stays fitted. On a journey inside one neighbourhood there is nothing to follow, and offering the choice invited the worse option.
- The camera's zoom is floored at the whole-route fit, so following can never show *less* than fitting. Without that floor a journey the camera window already spanned produced a follow frame of the whole route but with heavier padding and a floored zoom, so **Follow bug** came out strictly wider than **Whole route** — and, having nothing to re-frame for, never moved either.
- **Follow bug** and **Whole route** are two states of one camera, shown as a segmented control rather than two buttons. Follow comes first because it is the initialized default and the legible documentary view; Whole route is the honest overview. They were separate buttons before, and Fit route cleared only `followActive`, so the next scrub silently re-engaged the camera and undid the fit. Choosing whole route now clears `followEnabled` and sticks until follow is chosen again; clicking the selected segment re-fits, since the map may have been panned.
- Journey initialization must apply the selected camera mode. `buildMapJourney` used to call `fitRoute(false)` unconditionally, so a fresh page said **Follow bug** while showing the whole route; it now updates camera availability first, then activates Follow or fits Whole route to match the control. The initial Follow frame snaps into place without a fly-in from the world view.
- Whole route must fit the route geometry, not just its cache coordinates. `routeFitBounds` includes the ±180° endpoints inserted by `splitAtAntimeridian`, and Leaflet's minimum zoom is 0 so a globe-spanning route can actually fit in a phone-width pane. The previous minimum of 2 made a full-world fit physically impossible and clipped the route and bug at the horizontal edges. Regional views centre the route inside the control/player-safe area; globe-spanning views prefer the equator so the non-wrapping north and south map edges stay visually balanced, then clamp that centre just enough to keep every stop in the safe area. A globe-scale pane can expose part of an adjacent wrapped world, so `showNearestBugCopy` keeps only the copy nearest the camera visible rather than showing a second clipped bug in the margin.
- Extreme-value stats name the stop they came from, with the cache code linked and its region and country beside it. A bare "121.8° E" says nothing a reader can act on. Longest hop names both ends, since a distance between one named place and nowhere is not a distance.
- Comparison tiles carry a minimum mileage and disappear below it: the Moon needs 1% (2,389 miles), coast to coast one crossing (2,790), marathons five (131), and the times-around-the-world note a tenth of a lap (2,490). Without them a six-mile journey reported "0%", "0×" and "0 marathons".
- The countries, states and continents tiles open the same list dialog as the player readouts, but over the whole journey rather than progress so far, since the tiles are totals.
- The map camera holds a frame and only re-frames when the bug leaves it. Framing the next few stops instead meant chasing all 2,472 caches, which was exhausting to watch. It now takes the bounds of a `CAMERA_LOOKBEHIND`/`CAMERA_LOOKAHEAD` window, holds that frame until the bug crosses out of it (`CAMERA_FRAME_INSET`), then glides to the next. On the large fixture that is 43 re-frames across a 150-second playback, roughly one every 3.5 seconds, with the bug never leaving the screen. **Follow** in the map topbar toggles it; **Fit route**, dragging, wheel-zoom and double-click all disengage it.
- `CAMERA_STYLE` switches between `"cut"` (current) and `"glide"`. Cutting removes continuous motion altogether. Its cost is rhythm: re-framing arrives in bursts rather than at a steady rate, so on the large fixture 48 of 82 cuts land less than a second after the previous one, between holds as long as fifteen seconds.
- Padding the held frame to reduce cuts was tried and reverted at 0.35: the frames grew past what the minimum zoom can display, so the frame no longer fitted the viewport and the bug fell outside it. A small 0.08 slack remains.
- Widening the frame after a short hold was tried and reverted. It feeds back on itself: a frame too wide to fit at the minimum zoom leaves the bug outside it, which forces another re-frame, which widens again. It ran away to a whole-world view with the bug off screen half the time. Any future attempt at evening out the rhythm has to hold the *old* frame longer rather than enlarge the new one.
- Discrete jumps snap the camera, playback eases it. The camera only advances when progress does, so scrubbing to a distant stop and releasing would strand it part-way through a glide.
- Playback supports play/pause, timeline scrubbing, 1×/2×/4× speed, direct stop selection, and route fitting.
- The countries and states readouts open a list of the places reached so far, in first-visit order with a per-place cache count. They are buttons, not statistics: whatever the readout counts, the dialog must enumerate exactly.
- Journey log markers carry a pin, not the stop number. Four-digit numbers overflowed the circle, and the stop number already sits at the end of the row.
- Hovering a stop marker turns it the legend's green, which answers "what is this dot" against a trail that colours recent stops coral.
- Recently visited stops fade from coral back to green across `MARKER_TRAIL` stops, leaving a wake behind the bug. Recency is measured in stops, not seconds, so the trail survives pausing and scrubbing; only markers inside the window are touched, so the work stays bounded. A `--recency` custom property drives a `color-mix`, which means no per-marker class churn.
- The journey heading carries whole-journey totals under the description, distinct from the player's running counts: miles, countries, US states, continents and unique caches. Mileage leads because it is the number this community measures trackables by, and it carries the "times around the world" comparison that gives a six-figure figure meaning. Tiles reading zero are dropped, so a journey inside one country shows neither states nor a second continent.
- The player card is the single home for live journey state. Miles traveled and countries sit beside the timeline that drives them, because both readouts advance with playback. There is no sidebar statistics grid.
- Mapped-stop count and travel duration are deliberately absent: the player already says `Stop N of M`, and KML has no dates to measure duration from.
- The timeline is labeled Start / Latest, or three years when the journey has dates. "Halfway" was dropped: it marked the middle of the scrubber, not the middle of anything a reader navigates by, while the years are real data and stay.
- There is no map legend. The timeline track carries the route ramp and the playhead travels it, which explains the chronology without a chip floating over the map. The trade is that the ramp is only revealed as playback advances, so a first look at an unplayed journey has no colour key.
- The journey log has two groupings, switched by the **By time / By place** toggle. **By time** is the flat chronological list; **By place** is a country → state → caches tree. Both stay available because the log is the only place the ordered sequence can be read, while the map and timeline carry chronology on their own.
- Place rows show a flag, the name, and the count on the right. Do not add a per-row cache-count subtitle: it repeats the number already at the end of the row. The only inline detail is the state tally on the United States.
- The place tree groups by aggregate, not by visit. The large fixture crosses borders 66 times, so a row per visit would mean 67 country rows and 140 state rows including 36 single-stop blips; aggregating gives 22 and 19 clean rows. Countries and states are ordered by first visit, so the list reads as an itinerary and matches the route ramp's direction.
- Only one country and one state are open at a time. That accordion is what keeps a leaf list windowable, since Washington alone holds 1,519 caches.
- Opening a place moves playback to its first stop; closing one leaves the bug where it is. The open group tracks playback, so the highlighted country follows the bug across all 66 crossings.
- Desktop uses a scrolling story sidebar beside the map.
- Mobile orders the workspace title and description, then the map, then the journey log, so the reader knows whose bug they are watching before they watch it. The sidebar becomes `display: contents` at that width so its two halves can be placed around the map; its padding and paper texture move onto them, since a contents box has none of its own. The cost is that the map now starts about 510px down, leaving roughly 330px of it in the first viewport.
- Below 780px the two camera modes use different map layouts. **Follow bug** keeps the tall cinematic pane and overlays a floating player. **Whole route** makes the map a full-width `8 / 5` responsive overview and docks an edge-to-edge player directly underneath it; leaving blue stage gutters around a second floating card made the overview look accidentally unfinished. Switching from Whole route to Follow bug animates the stage height for 420ms while the player narrows, lifts from the map edge, and rounds into its overlay position, then `flyTo` glides the camera into the bug's frame. Reduced-motion users get the immediate state change.
- Statistics shown during playback represent progress up to the current stop, not final totals.
- Share copies a link that actually replays the journey. `journey-link.js` packs the title, description, official mileage, cache codes and coordinates into the URL fragment, so nothing is stored and nothing reaches a server — the link is the data. The fragment is adopted into the address bar when shared, and cleared when a new KML is imported.
- Sharing has a hard ceiling of `MAX_URL_LENGTH` (2,000 characters), past which messaging apps, mail clients and QR codes mangle links. Over that, the button says the journey is too large rather than copying something broken. Roughly 450 stops fit; the description is dropped first, since it is the most expensive field and the route is the point.
- Shared links embed cache coordinates, which Geocaching withholds from signed-out users. This is a deliberate product decision, taken knowingly: the sharer downloaded the data legitimately and chooses to pass on their own bug's route. Do not widen it into bulk redistribution.
- Zoom sits as a vertical stack, **+ above −**, matching every other map. Zoom in comes first in the DOM so the tab order follows the visual order; the column is CSS. `.map-topbar` uses `align-items: flex-start` because the stack is twice the height of the controls opposite it and centring pushed those down to meet its middle.
- The camera window **scales with journey length** (`cameraWindow`). The fixed 8 behind / 45 ahead were tuned on the 2,472-stop fixture, where neighbouring stops are often metres apart and 45 ahead is a local cluster. On a sparse journey those counts span the whole route: an 18-stop bug crossing the Caribbean, Chicago, Hawaii and Switzerland framed stops `0..17` at *every* index, so the frame never changed, `viewHolds` was always true, and Follow bug looked identical to Whole route. Scaling keeps the dense case byte-identical (2,472 still yields 8 and 45) and gave that journey 5 distinct views, zoom 8 over Chicago out to zoom 2 for the Pacific.
- `fixtures/TBAR286.kml` (Benny the Burger) exists for exactly that shape: **18 stops, 18,972 miles, 166° of longitude**. The other two cannot catch it — one is dense, the other sits in a single neighbourhood. Journey *shape* needs covering, not just size.
- Watch for this shape generally: a guard tuned on one fixture can be silently wrong on another. `CAMERA_CONTROL_MIN_ZOOM` already caught journeys confined to a *small area*; it could not catch one that is globe-spanning but has *few stops*.
- `loadJourneyData` resets `followEnabled` to true for every journey. `updateCameraControls` turns it off for a route that already fits above `CAMERA_CONTROL_MIN_ZOOM`, and that state used to persist: loading Brassica then Captain Cookie opened the big journey on Whole route with no indication why. Camera state is per journey, not per session — reset it with `followActive` and the camera frame.
- The mobile camera toggle animates **both ways** through `transitionMapLayout(applyLayout, settle)`: it measures the stage, map and player card, runs the state change, measures again, and animates between the two. Follow bug had this and Whole route snapped, because only `transitionToFollow` existed and the fit button toggled the class directly.
- It decides whether to animate by **comparing the measurements, not the breakpoint**. Desktop serves one layout for both modes, so nothing moves and the animation is skipped; a re-fit while already on Whole route likewise re-frames without animating. That stays correct if the two layouts ever converge at some other width.
- Pressing play collapses an expanded log and scrolls the sidebar back to 0 (`scrollLogToTop`, smooth), so the **Journey log** heading and the By time / By place toggle return with it; the collapsed window puts the active card just beneath them anyway. `play()` used to zero `logExpandBefore`/`logExpandAfter` without rebuilding or scrolling, so a user who expanded earlier stops and browsed away resumed playback with the orange card thousands of pixels off screen.
- That collapse is anchored. Removing rows above the viewport shortens the document, and on mobile — where the page is the scroller, not the sidebar — the browser clamps the scroll and the whole view jumps (measured: 2,316px). Anchoring holds the card's on-screen position exactly; the deliberate scroll-to-top then runs on desktop only, because pulling the page down to the log on mobile would take the map off screen just as playback starts.
- The journey heading (title, description, stats, share) is a full-width row of the workspace card, spanning both columns above the log and the map; the sidebar holds only the journey log. It used to sit inside the sidebar, where at 1440x900 it measured **887px tall inside a 628px scroll container** — the 16 stat tiles wrapped into a 3-column grid 683px tall, and the journey log began 939px down, entirely below the fold. Moving it out put the log at the top of the sidebar and cut the stats to two rows.
- That heading costs vertical space the viewport does not have, so **the page scrolls** rather than fitting the viewport: at 1440x900 about 300px of the 630px map is visible on load and the page scrolls ~360px. This was chosen deliberately over shrinking the map or compacting the intro bar. Revisit if the map reads as too low.
- Prose in the heading is capped at `74ch`. Full card width would otherwise give the description a ~1300px measure.
- A stat tile that has more to say opens the shared detail dialog: `data-open="<kind>"` on the tile, dispatched through `openStatDetail`. `openDetail(eyebrow, title, rows)` renders every one of them — places, distance, reach — so there is one dialog renderer, not one per statistic. Rows carry either `sub` (plain text, escaped by the renderer) or `subHtml` (pre-built markup such as a linked cache reference), never both.
- **No tile carries a note.** Every secondary figure lives in that tile's dialog, so the bar is label-and-value only and all nine tiles are openable. Stripping the notes took the row from 87px to **72px** — the notes, not the values, were setting the height.
- The dialogs the notes became: **Caches** (mapped stops, caches with repeat visits, most returned to — three rows, not five: "different caches" was already the dialog title *and* the tile that opened it, and "return visits" is exactly stops minus caches, so it rides in the sub-line of the row it explains), **Typical hop** (typical vs average move, shortest move, and how many moves fell under a mile / over 100 / over 1,000), **Date line** (each crossing with its two endpoint caches). The typical-vs-average gap is the point of that one — 0.66 mi against 71 mi on the large fixture is local shuffling punctuated by rare flights.
- The distance dialog's eyebrow pluralises with its row count — "Other ways to picture it", or "Another way" when only one comparison clears its floor (a 200-mile journey clears the marathon and nothing else).
- Each opener is gated on having something to say: Caches only with return visits, Typical hop only above two stops, Date line only with a crossing, Miles travelled only when a comparison clears its floor. Brassica opens four of its eight.
- Notes inside those dialogs need **absolute floors, not just ratios**. "Pulled up by a few long flights" fired on Brassica, whose 0.3 mi average is indeed twice its median and involves no flight; it now also requires a 25-mile average.
- Distance comparisons (around the world, Moon, marathons, coast to coast) and the four furthest-direction extremes live in those dialogs rather than as tiles. As tiles they were 7 of 16, crowding out the totals people actually scan; the mileage figure and the reach span carry the headline and the detail is a click away. `distanceComparisons` returns only rows that clear their floor. The last two — **raccoons laid nose to tail, and Jimothys** (a real Ballard raccoon with short spine syndrome, viral July 2026; 1.5 ft is our estimate, no outlet publishes a measurement) — carry `min: 0` deliberately, so every journey has something: Brassica's 1.7 miles is 3,529 raccoons. They sit after the real comparisons so the useful figures land first and the joke is the payoff. Because they always qualify, the mileage tile is now always openable; the `comparisons.length` guard stays as a safeguard rather than a live case.
- `.journey-stats` tiles draw their own 1px separators with `box-shadow`, and the container background matches the tiles rather than `--line`. The container used to paint `--line` behind a 1px gap grid, which was invisible while the tiles filled every cell but showed as a grey slab once the grid was wide enough to leave a ragged last row. Tile count varies per journey, so no column count avoids this generally.
- The stats row is always exactly one tile tall: `grid-auto-flow: column` with `overflow-x: auto`, so it scrolls sideways rather than wrapping. A second row pushed the map further down the page, which is the thing the full-width heading is spending budget on. Tile minimum 125px desktop / 132px below 780px. On mobile this took the row from **673px to 124px**.
- The scrolling row carries its own affordance, because macOS overlay scrollbars disappear when idle and left nothing to see: an edge fade plus a chevron button on each side, both shown only when there is hidden content that way. `updateStatsScroll` sets `data-back` / `data-next` on `.stats-scroller`, driven by a scroll listener, a `ResizeObserver` on the row, and the header render. Fade and chevron together on purpose — the fade shows content is cut off, the chevron makes it actionable for a mouse with no horizontal wheel.
- Those chevrons are `tabindex="-1"` and `aria-hidden`. They duplicate something keyboard users already get for free (focusing a tile scrolls it into view), so putting them in the tab order would add two redundant stops before the journey log.
- Do not give the row a bare `1fr` grid track. `1fr` is `minmax(auto, 1fr)`, and the `auto` floor resolves to the scrolling row's min-content, which widened the whole card past the viewport on mobile instead of scrolling inside it. Both breakpoints use `minmax(0, 1fr)`.
- The hop dialog's shortest move names its two caches, and **skips moves that begin and end at the same cache**. The large fixture's true minimum was 42 ft from GCAJFEK to GCAJFEK — the trackable logged at one cache twice, so those few feet are GPS drift rather than travel. Excluding them gives 57 ft between two real Canadian caches.
- `stopReferenceHtml` carries a country flag before the place name, so every dialog that cites a cache — reach, longest hop, date line, most-returned-to — gets it from one place. The flag is skipped when `flagsRender()` is false: the country is spelled out right beside it, so the letter-pair fallback would read "FI Finland".
- The description is clamped to **two lines** with **Read more** sitting at the end of the second, and opens the full text in the shared dialog rather than expanding in place. Expanding inline pushed the map down the page, which is the space the fit-to-viewport layout exists to protect; five lines cost the map ~90px. The dialog carries a prose body (`#placesText`) beside the row list, and `dialogBody()` picks whichever is visible so one scroller, one set of fades and one scrollbar serve both shapes.
- `.description-more` must be declared **after** `.link-button`. That base class sets `background: transparent`, and at equal specificity the later rule wins — declared first, the gradient that masks the clipped text silently never painted and the label sat on top of the words.
- The value on each dialog row is the point of the row, so it is set like the stat tiles' numbers — 21px Georgia in `--ink`. It was 10px muted, which put it **below its own 11px sub-line** in the hierarchy. The dialog widened from 390px to 520px to carry it.
- Dialog lists show they scroll with a **panel-coloured overlay fade** at each end plus a persistent scrollbar, toggled by `updatePlacesScroll` through `data-top` / `data-bottom` on `.places-scroller`. The pure-CSS `background-attachment: local` trick was tried first and reverted: a background paints *behind* the rows, so it can only cast a shadow under them, which reads as a grey band rather than the text softening away. Fading the text needs an overlay above it, and that needs the scroll position. The fades inset 11px from the right so they never wash out the scrollbar, and `.places-list` carries `padding-right` so the rows and their rules clear it.
- `updatePlacesScroll` must run **after** `showModal()`. A closed `<dialog>` is `display: none`, so every measurement reads 0 and the list looks like it does not scroll.
- The chevron pattern from the stats row is deliberately not reused here: horizontal scrolling has no natural mouse gesture, vertical does.
- **Never set `scrollbar-width` or `scrollbar-color` on an element that relies on `::-webkit-scrollbar`.** Chrome ignores every `::-webkit-scrollbar` rule when either standard property is present, so `scrollbar-width: thin` silently cancelled the visible scrollbar it was meant to refine — measured as a 0px gutter, 9px once removed. `.journey-sidebar` still uses the standard properties on purpose; the two approaches cannot be mixed on one element.
- The reach tile shows the **places at the four extremes as country flags**, deduplicated, and separated by `·` **only when an entry carries state text** (`🇫🇮 · 🇲🇴 · 🇺🇸 WA, CA`). Separators are free once states appear — 95px either way — but cost 8px between bare flags, which is why the all-flag case runs them together (`🇫🇮🇲🇴🇹🇼🇺🇸`, 87px). US state abbreviations appear only when the extremes fall in **two or more** states, grouped under the single flag (`🇺🇸 WA, CA, OR, ME`): one state distinguishes nothing the flag and the dialog do not already say, and it costs 8px. A `·` chain between flags cost the same 8px, so the flags' own margin does the spacing. Deduplication is not optional: a bug that stayed in one state is its own furthest point in every direction, so Brassica printed four identical flags, and two of its four extremes are literally the same cache. Repeating the flag per state was measured at 118px against 95px grouped.
- Place **names** cannot be the stat — measured at 131px against 87px for flags, more than the whole longest-hop saving. They appear at 15px only as the no-flag fallback.
- Flag support is detected by painting `🇫🇮` with a black fill and reading back a chromatic pixel: colour glyphs ignore `fillStyle`, so chromatic means a real flag rather than the `FI` letter pair Windows shows. **A width comparison does not work** — the pair measures wider (22px) than the letters (17.8px), and the first attempt reported "unsupported" on a machine that supports them.
- Each flag is `role="img"` with the country as `aria-label`, so the tile is not silent to screen readers. The reach tile also drops the openable-tile underline: flags are `inline-block` and cannot carry it, and an underlined abbreviation beside a bare flag reads as a fault.
- `formatDegrees` takes a decimal count and `degreeDecimals` raises it to 3 below a 1° span. At a fixed 1 decimal, Brassica's modal showed `47.7° N` for both its northernmost and southernmost stop, and `122.4° W` for both east and west.
- The old reach tile headlined the **north-south span in miles** ("North to south / 2,638 mi / at its widest"), not degrees. It briefly read "Furthest reach / 38.2° / north to south" and was cut on 2026-08-31: degrees of latitude mean nothing to most readers, and "reach" did not say reach in what direction. Miles is already the unit in three neighbouring tiles. Latitude is used because it converts to distance at a near-constant rate; longitude does not. The dialog rows keep degrees, which read fine next to a named place.
- The stats row is a sibling of the title/share row, not inside it. While it sat in the flex child alongside the share button it stopped ~54px short of the card's right edge. `.journey-heading` is a block; `.journey-heading-top` is the flex row that pairs the prose with the share button.
- Endpoint references (longest hop's from and to) live in the dialog, not on the tile. That one tile's wrapped reference set the height of the entire row — removing it took the row from 124px to 87px, since every tile is as tall as the tallest. Anything multi-line belongs behind a `›`.
- Openable tiles are marked at rest, not on hover: mint tint, `--green-dark` label, `›`, and an underlined value. A hover-only affordance is invisible until the pointer is already on the tile and never appears on touch. The underline is scoped off `dd small` so it marks the value and not the note under it. The desktop minimum alone collapses to a single column in a phone-width card, turning 16 tiles into 16 rows.
- The timeline carries **no end labels** on an undated journey. "Start" and "Latest" only restated which way a scrubber runs. Dated journeys keep their year labels, which say something; `.timeline-dates:empty` collapses the row so it holds no space when there are none.
- **Desktop fits the viewport.** `.app-shell` is a flex column with `height: 100vh` above 780px, `main` and `.journey-workspace` flex to fill, and the map row is `minmax(360px, 1fr)` instead of a constant tuned against a layout that no longer exists. `min-height: 100vh` does **not** work here — the shell simply grows with its content, so `1fr` has no constraint to resolve against and the map row never shrinks. The shell also carries `min-height: 780px`: `.journey-workspace` needs `overflow: hidden` for its rounded corners, so past the map's floor a short window would clip the player card with no way to scroll to it. Below that height the page scrolls instead. Measured 1440×1080 → 730px map, 1280×900 → 540, 1280×760 → 400, 1100×620 → scrolls.
- Below 780px the two header actions become **44px round icon buttons** — the folder, and a `?` — so the wordmark and its tagline both survive on a phone. Their text moves to `aria-label`, since the visible label is `display: none` and would otherwise take the accessible name with it. Scoped to `.header-actions`: the empty state has the row to itself and keeps full-width text buttons.
- The `?` button uses the same folder glyph's sibling treatment but **the primary keeps its folder icon rather than switching to a `+`**. One action showing two different glyphs across breakpoints is harder to learn than one, and `+` means create, which this does not do.
- The counted-hop rows carry no "of N moves" caption. The dialog's own eyebrow already frames them, and the repetition down three consecutive rows read as filler.
- A button's label must be **one element**. `.primary-button` has `gap: 9px`, and a flex container wraps each contiguous text run in its own anonymous item — `Open <span>your bug's </span>KML` was three items, so the gap fell between every word. `.button-label` wraps the whole label so the gap only separates icon from text.
- The Open button's icon is an **open folder**, not the arrow-out-of-a-tray upload glyph it shipped with. That glyph means upload, on the one button whose surrounding copy promises nothing is uploaded.
- The speed control keeps its horizontal padding and cancels it with an equal negative margin, so the hover pill has breathing room while the S of "Speed" still lines up with the scrubber. Removing the padding outright aligned the text but clamped the pill to the letters.
- The speed control sits **below the scrubber** rather than inline beside the date, which makes it a control rather than a caption but adds a row: the card goes 92px to 107px on desktop and 115px to 137px on mobile, where it covers 26% of the map.
- **"Follow your bug about." is the empty state's headline**, not a line under the wordmark; it had sat in both places at once, competing with "Where has your bug been?" for the same idea. The wordmark carries a different tagline, **"For the bug about town."**, so the two lines play off each other rather than repeat. The `<title>` still carries the original.
- There are two page states, set by `setAppState` through `data-state` on `.app-shell`. **empty**: a full-height panel with a large "Where has your bug been?" and the two buttons beneath it, workspace hidden. **journey**: the heading is gone and the buttons sit at the right of the site header beside the wordmark. Dropping the standing intro bar took the desktop page scroll from ~360px to **90px**.
- The open button relabels with the state: **"Open your bug's KML"** while empty, **"Open another bug's KML"** once one is loaded. "Another" is both accurate and a warning — opening a file replaces the journey on screen rather than adding to it. `setAppState` writes the visible label *and* `aria-label`, because the mobile header hides the label and would otherwise strip the accessible name.
- `.app-shell` carries `data-state="empty"` **in the HTML**, and the workspace is hidden by `:not([data-state="journey"])` rather than `[data-state="empty"]`. `bootstrap()` awaits the share-link decode before calling `setAppState`, so for that window the attribute was absent, neither rule matched, and the workspace rendered its placeholder markup — a visible flash of "Scout's grand bugabout" on every load. Both halves matter: the attribute covers first paint, the `:not()` covers the attribute ever being missing.
- The empty state deliberately carries **no drop hint**. One was tried and cut: the screen is stronger at two lines than three, and anyone who drags a file onto a page is already guessing correctly. Drag and drop still works everywhere, and its overlay appears mid-drag.
- `.quiet-button` shares `.primary-button`'s box exactly — 48px min-height, `0 22px` padding, 9px gap, 999px radius, 13px type. Only colour, weight and the shadow separate them, so stacked in the empty state they read as one control in two strengths. It was previously 37px tall with 11px/15px padding, which made the pair look assembled from different kits. The mobile header's 44px circles override both.
- The buttons are **one element moved between two slots** (`#emptyActions` and `#headerActions`), never duplicated — the ids stay unique and every listener bound to them survives the move.
- `setAppState("journey")` runs **early in `loadJourneyData`**, before anything measures the map. The workspace is `display: none` while empty, so Leaflet would otherwise size its container at 0×0; `display` is discrete and flips at the *start* of an entry transition, so the container is measurable even while it is still transparent.
- The workspace fades without a transform. A transform would make it a containing block, moving the map's absolutely positioned controls and disturbing Leaflet mid-animation.
- Below 780px the primary label drops its middle (`Open your bug's KML` → `Open KML` via `.button-long`) and, **in the journey state only**, the wordmark hides to leave the bug mark. All three plus the wordmark do not fit a 375px row. The empty state has the header to itself and keeps the full wordmark.
- **TEMPORARY:** `START_EMPTY` in `app.js` boots into the empty state so it can be designed. Set it to `false` to restore the default journey. Shared links still load either way.
- **Open your bug's KML** and drag and drop are the only ways in, so **Open your bug's KML** is the primary action in the intro bar with **Where's my KML?** beside it. The two are written as a call and response: the primary names a file the reader may not know how to get, and the secondary asks their exact question back. There is no TB-code or URL field: a static frontend cannot resolve a code into a journey, and a search box that only ever answers "go download a file" reads as a broken feature.
- The **Where's my KML?** dialog is the canonical import instructions, including that geocaching.com sign-in is required before **View in Google Earth** returns a usable file. Keep it accurate when the import path changes.
- The dialog's trigger and eyebrow are framed as import help — **Where's my KML?** / *Finding your KML* — while the headline, *Bring your bug's journey to life*, states the payoff. Eyebrow names the task, headline names the reward; both used to describe the product while the content was four task steps, which is what made the old chain read wrong. An interim headline counted the steps out loud and was dropped so the copy does not have to be resynced when a step is added.
- Those steps say to use the **browser, not the Geocaching app**, because the app has no Google Earth export. Step 2 carries `assets/google-earth-img.png`, a screenshot of the Trackable Options menu with the item circled, since it is the one step people cannot guess. Displayed at 340px against a 608px source so it stays sharp on retina; keep the alt text describing the menu item, not the picture.
- User-facing copy never says "we" or "our". The app refers to itself as **Bugabout**, addresses the reader as "you", and states system behaviour plainly ("The file is read in your browser"). A first-person plural implies a team and a server behind the page, which contradicts the local-only promise the same paragraph makes.
- Instruction copy is deliberately plain and second-person. The former headline, "Every little traveler leaves a very long trail", was cut on 2026-08-31 as corny and untrue — plenty of trackables barely move. The replacement, "Follow your bug as it hops about", deliberately extends the header tagline "Follow your bug about" rather than introducing a second verb for the same promise.
- Imported files are read locally with `File.text()` and are not sent to a server. **Never describe an import as an upload in user-facing copy** — the import toast, the drop overlay and the import dialog all promise the opposite, and "uploaded successfully" would contradict a stated privacy guarantee. Say loaded, opened, imported, or read.
- `SHARE_STOP_GUIDE` (450) is a guide, not the limit. The real ceiling is `MAX_URL_LENGTH`, which title length and coordinate spread also move, so the share toast says "up to about 450 stops". Keep the hedge — stating 450 as exact would be wrong for a journey near the boundary.
- An unavailable control that has a reason worth reading uses `aria-disabled="true"`, not the `disabled` attribute, and shows the reason in the toast when clicked. The share button is the case in point: `disabled` fires no click event, so its explanation could only live in a `title` tooltip, which no touch user can reach and which renders as an OS tooltip outside the app's visual language. The disabled look is carried by explicit muted border, fill, and `color` (the icons stroke with `currentColor`), not by a blanket `opacity`.
- The Sound button carries two glyphs, swapped by its existing `.selected` class the way the play button swaps play for pause. Sound is **off by default**, so the muted glyph is the resting state; the button used to show a sounding speaker whatever the state, which read as "sound is on" the moment the page loaded.
- Toasts appear at the **top** of the viewport. At the bottom they covered the player card — its scrubber, readouts and speed control all sit along that edge, so a toast reporting on playback obscured the thing it was reporting on. At the top it briefly overlays the wordmark instead, which costs nothing.
- Bind a figure to its unit with `\u00a0`. The import toast was wrapping between "176,530" and "miles"; non-breaking spaces move the break to a sentence boundary instead.
- The import toast reads `<Title> loaded: <n> stops, <miles> miles`. A colon, not an em dash: a long title wraps the toast, and the dash landed at the end of the first line where it read as a fragment. Dropping the punctuation entirely fails the other way — a short title fits on one line, and "Brassica loaded 6 stops" needs something between the clauses (2026-08-31). It deliberately carries no caveat about missing dates and log text: that is a permanent property of the KML format rather than anything about the file just chosen, it fired on every single import, and the import dialog already explains it.
- Never fabricate missing KML fields. Date-less imports use `Stop N of M`, and travel duration displays as unavailable; visit dates, log text, cache titles, and photos are never invented.
- Absent fields are omitted, not narrated. The importer leaves kicker and story empty rather than printing a sentence about what the format lacks.
- The story card is the active row of the journey log. During playback and navigation from outside the log, the window opens *on* the current stop rather than centring it, so the card sits at the top while the journey moves underneath it. A separate pinned element was tried and removed: it duplicated the stop that was already the first row.
- Clicking a stop that is already visible is different: it must turn into the orange card at the exact viewport position where it was clicked. `logWindowStart` pins the current virtual window, and the click-only `.selecting-stop` transition gently exchanges the two card heights while `animateStopSelection` holds the clicked row in place on every frame. Playback and outside navigation clear both the pin and transition, so fast playback never strobes the log. Rebuilding around the clicked index made the card jump to the top and often out of view, which was disorienting. Reduced-motion users get the immediate anchored state instead.
- Log transitions are for user actions only, never for playback. Expanding a gap or switching grouping animates; the signature-driven rebuilds that happen as the bug moves must stay silent, or a journey advancing 16 stops a second would strobe. `rebuildLogWithTransition` is the only path that animates.
- Expanding earlier stops measures the active card's on-screen position and restores it after the rebuild, adjusting the sidebar's scroll on desktop or the window's on mobile. Without that anchor, inserting 64 rows above the card shoves it off screen and the reader loses their place.
- Gap rows in the log are buttons. While paused they pull another `LOG_WINDOW` of stops into view in either direction; starting playback or loading a journey resets the expansion, because the window's normal job is to follow the bug.
- The selected row *is* the card: `.stop-item.active` carries the warm treatment, the larger serif code, the location, the cache-link arrow, and the kicker and story text when the journey has any. Every row renders its story markup hidden, so revealing it is the class toggle that already runs and playback still never rebuilds the list. Do not reintroduce a card that has to be moved between rows.
- In **By place** mode a collapsed country has no row to style, so the selected stop shows only as a highlighted place row. The player carries the current stop in both modes.
- A trackable description clips to five lines behind **Read more**. The full text is always in the DOM and clipped by height, so opening it is an animatable transition rather than a text swap. Whether the toggle appears is measured from actual overflow, not guessed from a character count, and the clip height must be expressed in the paragraph's own font metrics — `em` on the wrapper resolves against its inherited size, which silently made "five lines" six and a quarter.
- The readout row holds equal columns with a reserved width. Sized to content, it resized on every mileage digit and shunted the countries and states readouts sideways as the bug travelled.
- On mobile the speed control stacks under the play button, which narrows the transport cluster from 96px to 46px and gives the location line the room it needs.
- State is derived the same way, for US stops only. The states readout appears only when a journey actually enters the United States, and counts distinct US admin-1 units — a journey through Île-de-France and Attica has regions but no states.
- `region` holds the admin-1 name alone and must never bake in the country; the story card composes `region, country` itself. The sample data was rewritten to follow this.
- Country is the one exception, and only because it is derived rather than guessed: `country-lookup.js` resolves it geometrically from the stop's own coordinates. It must never overwrite a country that arrived with the journey, and if the boundary data cannot load the statistic falls back to the unavailable state rather than showing a number.
- The countries / states / continents dialog lists places **by distinct caches, descending** ("Most caches first"), not by first visit. First-visit order buried the substance — Canada, the second-biggest destination, sat third behind a country with a fifth of its caches. The sort is stable, so places tied on count keep first-visit order as the tie-break; the large fixture has six such ties.
- That count is **distinct caches, not stops**. It previously counted every visit, which read as a cache count while contradicting the Caches dialog's own split between "Different caches" and "Mapped stops" — the United States showed 1,936 where it has 1,893. Only two countries in the large fixture have any repeats at all, so the two orderings barely differ; the count is the part that was wrong.
- `visitedPlaces` has exactly one caller, `openPlaces`. The **By place** journey log is built by `buildPlaceTree` / `placeListHtml` and stays chronological on purpose — that view is a timeline, not a ranking.
- Provenance for that derivation lives in the countries readout's tooltip and in the **Where's my KML?** dialog, not in the readout text. An inline `· derived` suffix was tried and removed as visual noise in a compact readout. Keep both of those explanations in place; do not let derivation become entirely unexplained.

## Visual direction

- Warm paper background, deep evergreen ink, coral movement accents, yellow highlights, and muted blue-green map treatment.
- The route is colored by chronology, oldest to newest, along the `ROUTE_RAMP` stops in `app.js`: indigo `#3a2a9e` → blue → cyan → green → yellow → orange → crimson `#d92d5e`. It travels roughly 547 degrees of hue. Two narrower ramps came before it — plum-to-coral, then indigo-to-orange at about 95 degrees — and both left most of a long journey looking the same colour, because a warm-only sweep has nowhere to go in the middle.
- The ramp passes through green, which is also the cache-marker colour. They stay apart because the route green is much lighter: the nearest band sits 97 units from the marker's `#1f684f` in RGB. Keep that separation if the ramp is retuned. Purple is used because every other hue is already spoken for — green is the cache-stop markers, coral is movement and the bug, yellow is the bug marker — so an on-palette green ramp would blend old route segments into the stop dots. The stops are hand-picked rather than interpolated end to end, because a single purple-to-orange interpolation passes through a muddy grey-brown at its midpoint.
- `app.js` publishes that ramp once as the `--route-ramp` custom property. The map bands and the timeline track fill both read it, so the scrubber *is* the route's legend. Do not hardcode the ramp a second time in CSS.
- Georgia is the display face; system sans-serif fonts handle interface copy.
- Use rounded, tactile controls with restrained shadows. Avoid generic blue SaaS styling.
- Zoom lives in the map topbar with the other controls, as our own buttons; Leaflet's built-in `zoomControl` is disabled. They must call `disengageFollow` like any other manual map interaction.
- The map topbar splits into two groups: zoom on the left, view controls on the right.
- Playback can be scored with Flight of the Bumblebee, chosen from a menu on the map topbar and off by default, because unannounced audio is hostile and browsers block it before a gesture anyway. Tempo follows playback speed but is capped at 2x; beyond that the piece smears.
- Six synthesised voices are offered so the timbre can be compared in place. What separates them from sounding like a synth is not the waveform: detuned pairs, a filter envelope that closes as a note decays, a shared convolution reverb, per-note dynamics with an accent every fourth note, and a little timing scatter. A single oscillator with a fixed envelope will always read as electronic no matter which waveform it uses.
- The music is **synthesised from note data, never a recording**. Rimsky-Korsakov's composition entered the public domain long ago, but every recording of it carries its own separate copyright. Synthesising sidesteps that entirely and keeps a binary out of the repo. Do not swap in an audio file without clearing its licence.
- The map topbar carries only its controls. A "Journey view" label used to sit opposite them, but there is no second kind of view for it to distinguish.
- Preserve OpenStreetMap attribution whenever its tiles are displayed.
- Respect `prefers-reduced-motion`.

## Confirmed gotchas

- Leaflet JavaScript is not enough: its base stylesheet supplies essential pane positioning. When the external stylesheet failed to load, markers rendered thousands of pixels below the map. Keep Leaflet CSS local and load it before `styles.css`.
- The vendored data files carry their own `DATA_VERSION` in `country-lookup.js`. `index.html` only cache-busts the authored CSS and JS, so regenerating `countries.json` or `us-states.json` left browsers serving the previous copy — continents silently came back empty until the version was added. Bump it whenever `tools/` regenerates a boundary file. The same trap catches fixture KML during testing.
- The in-app browser can retain a stale static stylesheet after a normal reload. The current HTML uses cache-busting query strings for authored CSS and JavaScript. Increment them when a verification reload demonstrably serves stale assets.
- At ordinary 1280×720 desktop sizing, a large minimum workspace height pushes the player below the fold. The desktop workspace currently uses `height: calc(100vh - 270px)` with a 450px minimum.
- At 390×844, **Follow bug** uses `64vh` with a 520px minimum so the player ends just above the initial viewport edge. **Whole route** overrides that fixed height with an `8 / 5` map and a player below it.
- Leaflet map tiles still require network access even though Leaflet itself is vendored.
- The follow camera multiplies tile consumption. One full playthrough of the large fixture in Follow bug fetches **369 tiles** against 18 for a static view, and OpenStreetMap's public servers returned HTTP 429 during camera testing.
- Read the OSM policy accurately before citing it. It forbids **bulk downloading** — "pre-emptive fetching of tiles other than those a user is actively viewing" — and warns that heavy use "may be blocked without prior notice"; it sets no numeric limit and says "we welcome creative uses". Bugabout fetches only what the camera is showing, so it complies, and low-traffic personal use is within both the letter and the spirit. The risk is **volume-based blocking if it gets popular**, not a licence violation, and the failure mode is the map dying for everyone at once. Treat a provider swap as the thing to have ready, not a precondition for deploying.
- Do not read tile health from the Resource Timing API. Cross-origin images without `Timing-Allow-Origin` report `encodedBodySize: 0` whatever the outcome, so healthy OSM tiles look identical to failed ones. Check `img.complete && img.naturalWidth > 0`, or fetch a tile directly, before concluding anything is broken.
- Changing zoom drops the old tile level before the new one paints, and with nothing behind it the bare container showed through as a grey flash. A second tile layer capped at `maxNativeZoom: 4` sits underneath, blurred, so the gap shows an out-of-focus map instead of flat colour. Its handful of low-zoom tiles are cached after first load. The container colour was also retuned to sit closer to how tiles look once the tile-pane filter is applied.
- A tight, held camera frame is not achievable on the large fixture, and this was measured rather than guessed. Holding only on places where the bug lingers leaves it off-screen for 30-50 seconds of playback; splitting on relocation jumps gives 58-108 camera moves; a frame wide enough to move only every couple of seconds spans about 4,000 km. The cause is that playback advances by stop index, so the bug hops Seattle to California and back within one run. Raising the playback duration helped far more than any camera tuning did.
- `map.setView` resets the entire viewport and costs roughly an order of magnitude more than the rest of a frame: measured at a realistic 1× cadence on the large fixture, per-frame work went from 0.8 ms to about 8–9 ms once the camera was moving. `updateCamera` therefore skips moves below `CAMERA_MIN_PIXEL_STEP`/`CAMERA_MIN_ZOOM_STEP` (a converged camera costs 0.23 ms) and throttles to `CAMERA_MIN_INTERVAL`. Easing is expressed against elapsed time, not frames, so the glide does not change speed with the frame rate.
- `getBoundsZoom` takes the *total* padding, so a centre shift for asymmetric padding must be exactly half the top/bottom difference. Regional whole-route views apply only that correction. On globe-spanning routes, centring the mostly northern or southern stops exposes Leaflet's hard vertical world edge while longitude still wraps; `wholeRouteView` instead starts at the equator and clamps it to the range of centres that keeps the route inside the padded viewport.
- Leaflet invalidates its size on a browser resize but preserves the old centre and zoom; that is not a responsive fit. A `ResizeObserver` on `#map` calls `scheduleMapLayout`, which re-fits an overview and rebuilds an active follow-camera frame. It also catches the mobile camera-mode switch, where CSS changes the map's height without changing the window. While `.map-layout-transitioning` is active it only invalidates Leaflet's size; rebuilding the follow frame on every animation step would stutter and cancel the finishing camera glide.
- The Follow layout has a 520px minimum height, which clamps a Web Animation `height` keyframe too: animating from a shorter Whole-route stage still starts at 520px and visibly pops. `.map-layout-transitioning` temporarily releases that minimum after the real Follow target height has been measured. The Whole-route player is full width while Follow is inset, so the transition also animates the player's horizontal scale, bottom inset, radius, and shadow; changing only its relative/absolute positioning would make the card snap inward before the map began growing.
- Leaflet marker z-index is the marker's pixel Y plus its `zIndexOffset`. Stop markers offset by `100 + index`, which on the 2,472-stop fixture reaches 2,755 and buried the bug's original offset of 1,000, so the caches drew over the bug they were being visited by. `BUG_Z_INDEX` now sits far above both terms.
- Stops the bug has not reached yet, and the dashed route preview, sit at low opacity and fade out entirely while playback runs, returning when it pauses. They stay clickable when visible so the map remains a way to jump through the journey; on a long journey showing them all at full strength made the map unreadable.
- The player's location line reads `region, country • CODE`, with the code linking to the cache. Leading with the place keeps the line steady across a run of stops in one area instead of rewriting itself at every cache.
- The places dialog reuses `.about-dialog` for its panel, so any bare element selector under that class reaches it too. A `.about-dialog ol` rule meant for the import dialog steps indented the country rows and imposed a grid gap on them; those step styles now live on `.dialog-steps`. Scope by class, not by tag, in a shell two dialogs share.
- `.player-meta > span` styles the "Stop N of M" line and `.player-position > strong` styles the location. Keep those selectors scoped: a broad descendant rule also catches the bullet span inside the location line and stacks one line into three.
- Animating `max-height` to a generous fixed bound looks instant: the element stops growing at its content height, so the visible part of the transition is over in the first few percent. Measure the content and set the height, then release it to `none` afterwards so later reflows are not clipped.
- A `transitionend` listener registered with `once: true` is consumed by whichever property finishes first — here the `::after` opacity beat `max-height`, so the release never ran. Filter the property and remove the listener by hand.
- CSS transitions and `requestAnimationFrame` do not advance while the preview pane is hidden, so a faded element reads back at its starting opacity even though the rule applies. Non-animatable properties in the same rule (`pointer-events`) do take effect, which is the tell. Disable the transition to verify the target value.
- The whole journey log bleeds outward by `--card-bleed` and every row pads the same amount back in, so a hovered row and the active card fill an identical box with the same corner radius. Bleeding only the active card left the hover highlight a different width, a different radius, and pushed the card's right edge inside the row's. The connector line and the cache link are both positioned against the row, so both offsets include the bleed.
- The journey log's connector line is an absolutely positioned `::before` inside each row, so nested rows must be indented with `margin-left`, never `padding-left`, or the line detaches from the markers. Depth is carried by explicit `depth-1`/`depth-2` classes rather than sibling selectors, and the connector uses `:has(+ .stop-item)` so a group's last row does not trail a line into the next place header.
- The journey title is plain text, and **`meta.publicCode` no longer exists anywhere** — importer, share-link payload, or journey meta. It was derived from the *file name*, which only carried a TB code for fixtures we renamed ourselves, so it was null for every genuine upload and nothing rendered it. Dropping it from the link format is backward compatible: the decoder already treated the trailing field as optional, so links made before the removal still decode and their extra bytes go unread. Do not reintroduce anything depending on a TB code before the API milestone makes one genuinely available.
- **A real Geocaching KML download is named with a GUID, and contains no TB reference anywhere.** Confirmed 2026-08-31 against two genuine downloads: `57c0a7d5-e0b2-4f87-84ab-3e334b1d2db7.kml` and `ac894e2d-8a69-47fb-be8a-f2a95b1ee368.kml`, byte-identical in content to the fixtures but for the name. The fixtures in `fixtures/` were renamed to their TB codes by us for legibility; that is our convention, not Geocaching's. `parsePublicCode` reads the file name, so `meta.publicCode` is null for essentially every genuine upload, and anything depending on it — the journey title's link to the trackable page — must degrade silently. Do not build a feature that assumes the code is available.
- Official trackable KML is a map export, not a full activity log. Confirmed fields are document title, trackable description, official total miles, ordered cache codes, coordinates, and a route line. It omits visit dates, event type, log text, cache titles, photos, country, and region.
- The anonymously accessible trackable details page is the inverse: it provides rich history but no coordinates. Confirmed fields include date, action type, cacher, cache name/code when location-linked, displayed region, per-log URL, and public log text. It paginates at 10 records per page, newest first. Its **View Map** page and anonymous cache pages withhold coordinates until login.
- A trackable history record is not always a new position. For `TBBDVQG`, the public page has 22 records but only 6 `Dropped Off` events; the other records include retrievals, discoveries, and notes. Those 6 drop cache codes match the KML's 6 mapped stops in reverse chronological order. Model a narrative event stream separately from the position-changing route.
- The KML may have fewer mapped stops than the trackable page has history records because logs without usable coordinates are omitted.
- Duplicate cache codes are valid return visits and must retain their order. `TBA5TD9.kml` currently has 2,472 mapped stops and 2,428 unique cache codes.
- Some routes cross the antimeridian. When adjacent normalized longitudes differ by more than 180 degrees, end the departing segment at one ±180° edge and resume at the opposite edge at the same interpolated latitude; simply starting a new part drops the visible flight into and out of that region. Interpolate animation over the shorter longitude delta. Because Leaflet repeats basemap tiles but not vector layers, repeat every route part and maintain animated-bug copies at longitude offsets −360°, 0°, and +360°; only the copy nearest the camera is visible. Stop markers instead unwrap once against the visible world copy. The large fixture crosses into Asia between stops 2,178–2,179 and back out between 2,218–2,219.
- The traveled route is drawn as up to `ROUTE_BANDS` (24) polylines, not one. Beyond enabling the chronological ramp, this fixed a real cost: `updateRoute` used to rebuild the entire traveled path every animation frame — `journey.slice(0, i + 1)`, antimeridian splitting, then three world copies, roughly 7,400 coordinate pairs per frame near the end of the large fixture. Measured over a 400-step sweep of that fixture, per-update cost fell from mean 10.3 ms / p95 24.7 ms / max 101.6 ms to mean 0.7 ms / p95 1.6 ms / max 7.9 ms, moving every frame inside the 16.7 ms budget. A frame now rewrites only the active band; all bands are re-settled solely when a band boundary is crossed or the timeline is scrubbed backwards.
- Adjacent route bands must share their boundary stop, or a visible gap opens at each colour change.
- The journey link's format version must be written with `writeVarint`, not pushed as a raw byte. The reader zigzag-decodes every integer, so a raw `1` comes back as `-1` and every link silently fails its own version check and falls back to the sample.
- Stop markers follow the viewport, not a fixed sample. Sampling the whole journey drew only every 21st stop of the large fixture — 119 of 2,472, under 5% — so the route bent through caches with nothing to click. `refreshStopMarkers` runs on `moveend` and draws the stops actually in view up to `MARKER_BUDGET`, sampling within that set only when the view holds more. Zooming in therefore reveals every stop in view.
- Markers are drawn once at the longitude of whichever world copy is on screen, unwrapped against the view centre, rather than three copies of each. The route still needs all three copies; the markers do not.
- Marker coverage and the current-stop highlight have to agree. The highlight resolves to the nearest drawn index, since the exact stop may not have a marker when the view is sampling.
- Map buttons drop their labels on mobile with `font-size: 0`, which leaves the flex `gap` behind and pushes the icon off centre by half of it. Zero the gap as well, or the icon-only button is visibly lopsided.
- In Follow mode `.player-card` is anchored at the bottom inside `.map-stage`, so its bottom edge does not move when the card grows. Adding rows pushes its top upward and eats into map content. In mobile Whole route it is deliberately `position: relative` below `#map`; its margin must stay equal to Follow's inset (20px below 780px, 10px below 560px), because that geometric match is what makes the mode transition continuous.
- The journey log rebuilds from a render signature, not on every stop. The signature covers structure only — mode, which places are open, and the leaf window — while active-row highlighting stays a class toggle, so playback never re-renders the list.
- Long histories need bounded DOM and Leaflet work. The current renderer virtualizes the journey log to a 64-row window and draws only stops inside the padded viewport, capped at `MARKER_BUDGET` (260). Each drawn stop appears in the visible world copy once, while route parts and the animated bug repeat across three world copies.
- Country containment alone is not enough for geocaching coordinates. 272 of `TBA5TD9.kml`'s 2,472 stops fall outside every 50m polygon, and every one of them is a shoreline cache: 147 within 1 km of a coast, 119 within 1–5 km, 6 within 5–25 km, none mid-ocean. `country-lookup.js` therefore snaps an uncontained point to the nearest boundary within `COAST_TOLERANCE_KM` (25 km). Without it, 11% of that journey loses its country and Macao and Monaco vanish from the trip entirely.
- State boundaries come from Natural Earth's 10m admin-1 set, not 50m. Countries tolerate coarse data because they are large and mostly separated by coastline, which is why every unmatched stop was harmlessly out at sea. States share long land borders that follow rivers and roads — exactly where caches cluster — so 50m generalization would misassign stops near state lines. For the same reason `STATE_TOLERANCE_KM` is 10 km rather than the coastal 25 km: a generous snap is safe when the alternative is ocean and wrong when a neighbouring state is nearby.
- State lookup is gated on the stop's country already being the United States. Without that gate, `us-states.json` containing only US polygons would happily snap a Canadian stop to the nearest US state.
- Scanning all 1,620 country polygons per stop cost about 1.4 seconds for the large fixture. A 10-degree grid index cut it to roughly 450 ms. A finer 5-degree grid changed nothing, because the remaining cost is walking the segments of large rings, not choosing candidate polygons.
- Enrichment yields between time slices so playback never drops a frame, but only while `document.hidden` is false. A hidden tab clamps timers to about one second, which turned the same 450 ms pass into more than a minute during testing.

## Next engineering milestone

Enrich imported journeys without weakening the KML-first privacy and data-integrity rules. Keep acquisition separate from presentation so KML/GPX, an approved API, and fixtures all produce the same internal stop shape.

Implementation order:

1. `tools/run-smoke-tests.mjs` reads its pass count off the page. It used to print a hardcoded "7 tests passed" regardless of how many ran, so an added test looked like it had not been picked up. Keep the browser smoke suite current as importer, boundary, and share formats change; add full renderer regression coverage when the runner begins inspecting the application DOM.
2. Decide whether a GPX export contributes history data that KML lacks before adding it.
3. Ask Geocaching HQ for Authorized Developer access or express written permission for the proposed public-trackable-history use, and confirm the approved methods and fields.
4. Design the OAuth-backed direct-import adapter; do not scrape signed-in pages as the product data layer.
5. A shareable link now exists without storage: `journey-link.js` puts the journey in the URL fragment, so there is nothing to store, consent to, or delete. A *saved* journey format, which does store data server-side, still requires storage, consent, and deletion behavior to be defined first.

Expected normalized stop fields include:

- stable log or stop identifier
- event date and type
- public cache code and display name
- latitude and longitude
- region and country
- movement distance and cumulative distance
- public log URL
- optional story text and photo references

Before implementation, decide how to handle visits versus drops/retrievals, duplicate logs, missing coordinates, impossible jumps, archived caches, and active-cache location privacy.
